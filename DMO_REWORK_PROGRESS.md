# DMO Rework — Progress Log

Обновлено: 2026-09-18 (шрифты заменены на гарнитуры с кириллицей, см. §4.10)

## 1. Инвентаризация репозитория (Этап 0)

### Стек (проверено, а не предположено)
- **Vite 7 + React 19 + TypeScript 5.6**, SPA без SSR/SSG.
- Проверено отсутствие: `astro.config.*`, `next.config.*`, `remix.config.*`,
  `pnpm-workspace.yaml`, `apps/*`, `packages/*`. **Monorepo — нет.**
  Структура: `client/` (весь фронтенд), `server/` (express-статик, в билд
  больше не входит), `shared/`, `design/` (документация дизайна), `patches/`.
- Роутинг: **wouter** (`client/src/App.tsx`), единственный маршрут `/` → `Home`,
  остальное → `NotFound`.
- Стили: **один файл `client/src/index.css`** (165 очень длинных строк).
  Tailwind v4 подключён (`@tailwindcss/vite`), но сайт написан на
  **собственных CSS-классах и CSS-переменных** (`--lime #c7ff45`, `--ink`,
  `--paper`, `--ease-out`). Токены дублируются в `design/design-system.md`.
- Анимации: **чистый CSS** (`.reveal`, keyframes) + императивные
  pointer-обработчики в `Home.tsx` (кастомный курсор, magnetic, tilt).
  `framer-motion` в зависимостях есть, но **не используется**.
- UI-кит shadcn/radix (`client/src/components/ui/*`, 53 файла) присутствует,
  но лендинг его **не использует** — только `lucide-react` иконки.

### Пакетный менеджер / команды запуска
- `packageManager: pnpm@10.4.1`, есть `pnpm-lock.yaml`. Только pnpm.
- `pnpm install` — OK (6.3s).
- `pnpm dev` — vite dev server, порт 3000.
- `pnpm build` — `vite build` → `dist/public`. **Проверено: билд проходит.**
- `pnpm check` — `tsc --noEmit`. **Проверено: без ошибок.**
- `pnpm format` — prettier. Отдельного `lint` в проекте нет (ESLint не настроен),
  роль линтера выполняют `pnpm check` + `pnpm format`.

### Где лежит контент
**Весь контент захардкожен** в `client/src/pages/Home.tsx` (351 строка):
частично в локальных константах вверху файла (`navItems`, `services`, `team`,
`cases`, `faqs`), частично — прямо в JSX (hero, проблемы, процесс,
принципы, контакты, футер). Внешней CMS/API нет. Слой данных отсутствует —
его нужно создать (Этап 4).

### Карта секция → файл (подтверждена по коду, не по скриншотам)
Всё в `client/src/pages/Home.tsx`, стили — в `client/src/index.css`.

| Текущий экран | Якорь / класс | Строки Home.tsx | Новый блок |
|---|---|---|---|
| Hero «Делаем сложное понятным.» | `.hero` | 270–283 | 01 Hero |
| Бегущая строка | `.marquee-strip` | 285 | (сохранить, сменить слова) |
| «Бизнес растёт быстрее…» + Ясность/Скорость/Результат | `#about .problem` | 287–298 | 02 Проблемы B2B |
| «Двигаемся по делу.» 4 шага | `#process .process` | 300–310 | 05 Как работаем |
| «Собираем системы.» 4 карточки | `#services .services` | 312–316 | 03 Что разрабатываем |
| «Четыре оптики.» 4 досье | `#team .team` | 318–322 | 07 Команда |
| «Говорят цифры.» 3 кейса | `#cases .cases` | 324–328 | 04 Кейсы |
| «Меньше шума. Больше смысла.» 5 пунктов | `#why .why` | 330–333 | 06 Почему безопасно |
| «Спросить напрямую.» 4 вопроса | `#faq .faq` | 335–338 | 08 FAQ |
| «Есть задача?» + форма | `#contact .contact` | 340–344 | 09 Финальный CTA |
| Футер | `.site-footer` | 348 | футер |

Светлые секции (`background: #e7e9df`): только `.process` и `.team`.
Тёмные: `.section-dark` = `#101310`, базовый фон = `#0b0c0b`.

### Деплой
- **`vercel.json` отсутствует.** Последний коммит `453b016` убрал сборку
  express-сервера: билд = `vite build`, выход — `dist/public`.
- Express (`server/index.ts`) в продакшене не использовался; удалён при чистке,
  см. решение №13.
- Следствие: при добавлении маршрутов (`/cases`) прямая загрузка URL даст 404 —
  нужен SPA-rewrite. Это зафиксировано как задача Этапа 4.

### Аналитика (проверка по §9 ТЗ)
В `client/index.html` есть тег Umami с плейсхолдерами
`%VITE_ANALYTICS_ENDPOINT%` / `%VITE_ANALYTICS_WEBSITE_ID%`.
Переменные окружения **не заданы** — билд выдаёт предупреждение, скрипт
грузится с битым src. Фактически аналитики на сайте **нет**.
Итог: битый тег удалён; аналитику решили не подключать на старте, слой
событий оставлен провайдер-независимым (решение №11 в §5).

### Найденные дефекты (до начала работ)
1. **Битые фото команды.** Портреты Никиты, Матвея и Арсения ссылаются на
   `/manus-storage/*.jpg|png`, но каталога `client/public/manus-storage` в репо
   **нет** — в продакшене это 404 и пустые карточки.
2. Форма заявки ничего не отправляет (прототипный success-state прямо говорит
   об этом пользователю).
3. `<title>`/`description` в `index.html` описывают сайт как «дизайн-референс».
4. В футере выведено `DESIGN REFERENCE / PLACEHOLDER CONTENT`.
5. Кейсы (NORTH / KADO / SFERA) и их метрики (+38%, −24%, 2.7×) — вымышленный
   placeholder (прямо задокументировано в `design/README.md`).
6. Нет видимого focus-состояния для клавиатуры (кроме инпутов формы).

## 2. Статус по этапам

| Этап | % готово | Статус | Комментарий |
|---|---|---|---|
| 0. Аудит | 100 | done | Стек, контент, карта секций, деплой, аналитика проверены на коде, на билде и на живом рендере |
| 1. Планирование | 100 | done | Порядок секций, схема данных кейсов/команды, список файлов — см. §5 |
| 2. Перестройка структуры | 100 | done | `Home.tsx` разобран на секции, порядок блоков приведён к схеме ТЗ |
| 3. Контент по блокам | 100 | done | Все 9 блоков наполнены текстами из ТЗ |
| 4. Слой данных кейсов | 100 | done | `client/src/content/cases.ts` + `/cases` + `/cases/:slug` |
| 5. Форма заявки | 95 | done (нужен ключ) | Валидация, UTM, honeypot, rate-limit, success/fallback — проверены. Не хватает только env-переменной канала доставки, см. §4 |
| 6. Аналитика | 95 | done (нужен ключ) | События и scroll depth проверены на живой странице. Umami включается при заданных env, см. §4 |
| 7. SEO | 90 | done (кроме фото) | Title/description/OG/canonical/JSON-LD, sitemap и robots генерируются из слоя данных |
| 8. UI-качество | 100 | done | 1440 / 1280 / 390 проверены скриншотами и замерами, найденное исправлено |
| 9. Финальная проверка | 100 | done | `pnpm check` и `pnpm build` проходят, критерии приёмки — §8 |

## 3. Статус по блокам контента

| Блок | Готово (%) | Что сделано | Что не сделано и почему |
|---|---|---|---|
| 01 Hero | 100 | Новый H1/подзаголовок/микролейбл, два CTA («Разобрать задачу» / «Смотреть кейсы»). «Делаем сложное понятным» осталось вторичной бренд-фразой под описанием и в футере | — |
| 02 Проблемы B2B | 100 | Проблемная рамка + 3 карточки (`site.ts → problems`) | — |
| 03 Что разрабатываем | 100 | 4 продуктовых направления вместо агентских услуг, сетка сохранена | — |
| 04 Кейсы | 90 | 4 кейса с задачей, решением, функциями и доказательством; страница `/cases` и отдельные URL кейсов | Нет обложек проектов — используется исходная генеративная графика. Реальные скриншоты систем усилили бы блок |
| 05 Как работаем | 100 | 4 шага с «Ваш вклад» | — |
| 06 Почему с нами безопасно | 100 | 5 пунктов снятия рисков (заголовок + пояснение у каждого) | — |
| 07 Команда | 85 | Роли и описания обновлены, Арсений — Founder/Sales/Client Acquisition | Фото не в репозитории (§6), роли Никиты и Матвея — на подтверждении (§4) |
| 08 FAQ | 100 | 6 B2B-вопросов из ТЗ | — |
| 09 Финальный CTA | 100 | Новый H2/подзаголовок, форма по требованиям §8 ТЗ | — |

## 4. Открытые вопросы клиенту

### 4.0 Переменные окружения

Аналитику решили пока не подключать (решение №11 в §5). Осталась одна пара —
доставка заявок. Задаётся в Vercel → Project → Settings → Environment
Variables, окружения **Production и Preview**:

| Переменная | Для чего | Где читается |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | токен бота, которым отправляется заявка | `api/lead.ts` |
| `TELEGRAM_CHAT_ID` | чат, куда падает заявка | `api/lead.ts` |

Работают **только вместе**: без пары канал считается ненастроенным, функция
отвечает 503, а форма показывает fallback с готовым письмом — заявка не
теряется молча.

**Значения в репозиторий не кладутся.** Токен бота — секрет, он задаётся
только в дашборде Vercel. В коде нет ни `.env` с значениями, ни хардкода;
шаблон с именами переменных (без значений) лежит в `.env.example`.
Отдельно: у токена **не должно быть префикса `VITE_`** — такие переменные
Vite вшивает в публичный бандл.

Необязательные:

| Переменная | Для чего | Что без неё |
|---|---|---|
| `LEAD_WEBHOOK_URL` | альтернатива Telegram: заявка уходит POST-запросом на любой URL (CRM, Make, n8n) | не используется, достаточно пары Telegram |
| `VITE_SITE_URL` | канонический домен для canonical, OG и sitemap | берётся `https://digital-modus-operandi.vercel.app` |

Проверка после деплоя: отправить тестовую заявку через форму — должен
появиться экран «Заявка принята», а не предложение написать письмом.

### 4.10 Шрифты без кириллицы — ЗАКРЫТО 18.09.2026

**Проблема.** Space Grotesk (все крупные заголовки) и DM Mono (технические
подписи) не содержат кириллицы — проверено по сабсетам Google Fonts: latin,
latin-ext, vietnamese. Русский текст в них подставлялся системным шрифтом
посетителя, разным на Windows, macOS и Android. В одной строке получались два
разных шрифта: «B2B» фирменным, «-системы, которые забирают…» системным.

**Как выбирали.** Скачали файлы девяти гарнитур и отрисовали образцы с
настоящими строками сайта в реальных кеглях на фирменном фоне, затем замерили
ширины набора — чтобы решение опиралось на цифры, а не на впечатление.

| Шрифт | Ширина латиницы к текущему | Вывод |
|---|---|---|
| Unbounded | **+29,8%** | отклонён: разъезжается вёрстка, характер рекламный |
| Onest | −3,5% | **выбран** |
| Golos Text | −2,1% | подходит, но нейтральнее по характеру |
| Wix Madefor Display | −4,2% | подходит, но ближе к обычному UI-шрифту |
| Martian Mono | **+14,3%** | отклонён: подписи не влезают в контейнеры |
| JetBrains Mono | 0,0% | **выбран** |
| IBM Plex Mono | 0,0% | равноценная альтернатива |

**Решение: Onest для заголовков, JetBrains Mono для подписей.** Onest —
геометричный гротеск с кириллицей и латиницей в одной гарнитуре, держит
отрицательный трекинг и почти не меняет ширину набора. JetBrains Mono —
инженерный характер, различимые `0/O` и `1/l` (важно для `CASE_001` и
`BUILD_2026.09`), ширина буква в букву как у DM Mono. Manrope в основном
тексте оставлен: кириллица у него есть.

**Что изменено.** В `:root` заведены `--font-display` и `--font-mono`, на них
переведены все 43 объявления гарнитуры (21 дисплейное, 22 моноширинных) —
раньше часть была задана шорткатом `font:`, из-за чего замена в одном месте
была невозможна. В `client/index.html` обновлены запрос к Google Fonts и оба
`preload`; ссылки теперь ведут на **кириллический** сабсет, а не латинский,
потому что сайт русскоязычный.

**Проверено.** Шрифты подменены локальными файлами через перехват запросов
(прокси песочницы Google Fonts не пускает), страницы отрисованы в браузере:
горизонтального переполнения нет ни на 1440, ни на 1280, ни на 390;
контраст не изменился (6/0/0). Мобильный кегль заголовка уменьшен с
`clamp(2.3rem, 9.4vw, 3.5rem)` до `clamp(2.1rem, 8.6vw, 3.3rem)`: Onest
чуть шире, и строка «часть продаж на себя.» переставала помещаться, оставляя
висячее «себя.» отдельной строкой.

### 4.1–4.9 Вопросы по контенту
### 4.1–4.9 Вопросы по контенту

1. **«Доступные слоты: 02» — это реальное ограничение?** По ТЗ надпись
   оставляют только если ограничение настоящее, но удалять её без
   подтверждения нельзя. Поэтому она **оставлена как была**
   (`client/src/content/site.ts → brand.availableSlots`). Пока ответа нет,
   критерий приёмки №10 («нет искусственной scarcity») закрыть нельзя.
   Значение меняется/убирается одной правкой в `site.ts`.
2. **Роли Никиты и Матвея.** Готовых текстов в ТЗ нет. Переформулированы из
   того, что уже лежало в репозитории:
   - Никита — `FULL-STACK / PRODUCT / E-COMMERCE`;
   - Матвей — `PROJECT MANAGEMENT / PROCESS / COMMUNICATION` (из прежнего
     описания убран медицинский маркетинг: он не работает на B2B-позиционирование;
     факт «10 лет ведёт проекты» сохранён).
   Нужно подтверждение или свои формулировки.
3. **Фотографии команды.** Нужны файлы `nikita.jpg`, `matvey.jpg`,
   `arseniy.png` (и фото Бориса, если есть) в `client/public/team/`.
   Почему они пропали — см. §6.1.
4. **Канал доставки заявок.** Форма отправляет POST в `/api/lead`. Чтобы
   заявки доходили, в Vercel нужно задать `TELEGRAM_BOT_TOKEN` +
   `TELEGRAM_CHAT_ID` **или** `LEAD_WEBHOOK_URL`. Пока их нет, функция
   отвечает 503, а форма честно предлагает отправить письмо или написать в
   Telegram — заявка не теряется молча.
5. ~~**Аналитика.**~~ **Закрыто:** решили не подключать на старте, см.
   решение №11 в §5.
6. **Домен.** Канонические URL и sitemap собираются из
   `VITE_SITE_URL` (по умолчанию `https://digital-modus-operandi.vercel.app`).
   При переезде на свой домен переменную нужно задать.
7. **Дополнительные кейсы** (AGROHELP/AGRODEV, MUOVIS/ROBOTECH, HYDROMAP,
   ROBOTECH.MD). В ТЗ по ним есть только характеристика в одну строку. Чтобы
   собрать полноценные карточки, нужны 11 полей из шаблона §6.3 ТЗ. Пока они
   помечены `status: "draft"`: видны в списке «Другие проекты команды», но не
   на главной и не в индексации.
8. **Контакты.** `hello@dmo.agency` и `@dmo_agency` перенесены из прежней
   версии — нужно подтвердить, что адреса рабочие.
9. **Kaspersky internship** по ТЗ идёт в bio Бориса. В текущем описании его
   нет: нужна формулировка, которая не нарушает NDA.

## 5. Принятые решения и допущения

1. **Слой данных — типизированные `.ts`-модули** в `client/src/content/`
   (`types.ts`, `site.ts`, `services.ts`, `process.ts`, `trust.ts`, `team.ts`,
   `faq.ts`, `cases.ts`). Почему не CMS и не MDX: проект — чистый Vite SPA без
   content collections, а типы дают проверку на этапе `pnpm check` и
   используются сборкой для генерации sitemap.
2. **Кейс без доказательства не публикуется.** Поле `status: "draft"` держит
   такой проект вне главной и вне индексации — это защищает правило ТЗ «не
   выдумывать метрики» от постепенного размывания.
3. **Блок «Говорят цифры» заменён блоком кейсов.** Проверено: он выводил три
   вымышленных проекта (NORTH / KADO / SFERA) с придуманными метриками
   (+38%, −24%, 2.7×) — это прямо задокументировано в `design/README.md` как
   placeholder. Метрики удалены, карточка кейса получила поле `proof`
   (факт использования / финансирование / платящие пользователи).
4. **Единый CTA — «Разобрать задачу»** на всех экранах; «Смотреть кейсы» ведёт
   якорем на блок кейсов. Все CTA ведут в один сценарий — секцию `#contact`.
5. **Порядок секций** приведён к схеме ТЗ (01 Hero → 02 Проблемы → 03 Услуги →
   04 Кейсы → 05 Процесс → 06 Доверие → 07 Команда → 08 FAQ → 09 CTA).
   Чередование тёмных и светлых секций сохранено: светлыми остались
   «Как работаем» и «Команда».
6. **Форма отправляется в serverless-функцию** `api/lead.ts` (Vercel). Это
   единственный способ сделать серверную валидацию и rate-limit на статике.
   Антиспам без CAPTCHA: honeypot + проверка времени заполнения + лимит
   3 отправки/минуту на IP.
7. **`vercel.json` добавлен**: SPA-rewrite (иначе прямой заход на `/cases`
   отдавал бы 404), output `dist/public` и кэш-заголовки.
8. **Sitemap и robots генерируются на сборке** плагином в `vite.config.ts`,
   который импортирует `publishedCases`. Так список URL не может разойтись с
   реальным набором кейсов.
9. **Субагенты не запускались.** По правилу ТЗ («если блоки физически лежат в
   одном файле — выполняй последовательно сам») весь контент до этапа 2 лежал
   в одном `Home.tsx`, а после разбора работа свелась к переносу уже готовых
   текстов из ТЗ. Параллельный запуск дал бы конфликты по общим файлам
   (`index.css`, `SiteShell.tsx`, слой данных) без выигрыша.
11. **Аналитика на старте не подключается** (решение клиента от 17.09.2026).
    Причины: при десятках визитов в месяц агрегированные цифры не дают
    материала для решений, а Umami — self-hosted сервис, которому нужны
    сервер и база: для команды из четырёх человек это несоразмерные расходы
    на обслуживание ради счётчика.
    Что важно: **атрибуция от этого не страдает**. UTM-метки и referer едут
    вместе с самой заявкой (`lib/leads.ts` → `api/lead.ts`) и попадают в
    Telegram блоком «Источник». То есть вопрос «откуда пришёл этот лид»
    закрыт без аналитики; открытым остаётся только «сколько пришло и не
    написали».
    Что осталось в коде: слой событий (`track`, `initScrollDepthTracking`)
    и сами вызовы в компонентах. Он не требует переменных окружения и
    молчит, пока на странице нет провайдера. Загрузчик Umami, читавший
    `VITE_ANALYTICS_*`, удалён вместе с этими переменными.
    Как включить позже: добавить на страницу сниппет провайдера. Для Umami,
    GA4 (`gtag`) или Яндекс.Метрики (`ym`) правок в коде не нужно вообще —
    события начнут уходить сами. Для провайдера с другим API (например
    Vercel Web Analytics) — одна ветка в `track()`.

10. **Страница 404** переписана в фирменном стиле: раньше это был шаблонный
    светлый экран на английском, а с появлением маршрутов на него стало
    реально попадать.

12. **Тулинг генератора убран из продакшен-сборки.** Плагины Manus
    (`manus-runtime`, `debug-collector`, `jsx-loc`, прокси storage)
    применялись и в dev, и в проде: в `index.html` инлайнился скрипт на
    366 824 байта. Теперь они включаются только при `command === "serve"`.
    В dev всё работает как раньше, включая прокси `/manus-storage`.

13. **Мёртвый код шаблона удалён**: 51 из 53 shadcn-компонентов (приложение
    использует только `sonner` и `tooltip`), неиспользуемые хуки, `Map.tsx`,
    `ManusDialog.tsx`, `const.ts` с OAuth генератора, каталог `shared/`,
    `template.json` и express-сервер. Сервер удалён потому, что скрипт
    `start` был заведомо сломан — `dist/index.js` никогда не собирался,
    а раздачу статики делает Vercel.

14. **Мета-теги кейсов пререндерятся на сборке** (`scripts/prerender-seo.ts`).
    Раньше их ставил только клиентский хук, поэтому краулеры без JS и превью
    ссылок в мессенджерах видели у всех страниц метаданные главной.
    Полный рендер React не делается: берётся собранный `index.html` и
    подменяются теги. Причина — на билд-машине Vercel нет ни браузера, ни
    сети.

15. **Контраст доведён до WCAG AA** (4.5:1 для обычного текста). Тон каждого
    цвета сохранён, менялась только светлота. Глобальный `--border`
    намеренно НЕ трогали: тонкие разделители секций декоративны, их
    осветление изменило бы сдержанность макета; подчёркивание полей формы
    поднято отдельным правилом, потому что это граница элемента управления.
    Осознанно оставлены ниже порога: гигантские номера `01–04` в карточках
    услуг (чистая декорация, тот же номер продублирован в `SERVICE_01`) и
    подписи на плейсхолдере портрета (исчезнут вместе с плейсхолдером,
    когда появятся фото).

16. **Провайдеры, которыми никто не пользовался, удалены.** `<Toaster />`
    рендерил контейнер уведомлений, но `toast()` не вызывался нигде;
    `<TooltipProvider>` давал контекст, который ни один компонент не
    запрашивал; `ThemeProvider` проставлял классу `<html>` значение `dark`,
    хотя ни одной утилиты `dark:` в разметке нет, а тёмная палитра задана
    прямо в `:root`. Вместе с ними ушли `sonner`, `next-themes` и
    `@radix-ui/react-tooltip` — минус 78 кБ из JS-бандла (−21%).
    `components.json` оставлен: если понадобится готовый компонент,
    `shadcn add` доставит его поштучно.

## 6. Известные проблемы / риски

1. **Фотографии команды отсутствуют в репозитории.** Портреты ссылались на
   `/manus-storage/...` — этот путь обслуживает **только dev-сервер**
   (плагин `manus-storage-proxy` в `vite.config.ts`), в продакшен-сборку файлы
   не попадают. На Vercel карточки были пустыми. Сейчас пути ведут в
   `client/public/team/`, а при отсутствии файла карточка показывает
   плейсхолдер-досье вместо битой картинки. Файлы нужно добавить (§4.3).
2. **Заявки пока не доставляются** — не задан канал (§4.4). Поведение при
   этом предсказуемое: 503 от функции и предложение написать письмом.
3. **`/api/lead` нельзя проверить локально** через `pnpm dev` — Vite не
   поднимает serverless-функции. Проверено сценарно (мок ответов 200/503):
   success-state и fallback работают. Полная проверка — после деплоя.
4. **SPA без SSR.** Мета-теги проставляются на клиенте. Google это читает,
   но часть краулеров и части соцсетей — нет. В `index.html` лежат
   статические title/description/OG для главной, поэтому при шаринге главной
   всё корректно; для шаринга отдельного кейса потребуется пререндер.
   Полноценное решение — SSG/пререндер, это отдельная задача.
5. ~~**Express `server/index.ts` остался в репозитории**~~ — **закрыто**:
   удалён вместе со сломанным скриптом `start`, см. решение №13.
6. **Шрифты Google в песочнице не грузились** (перехват TLS), поэтому
   скриншоты сняты с фолбэк-шрифтом. Фолбэк шире Space Grotesk, так что
   отсутствие переполнений на скриншотах — оценка «сверху»; в продакшене
   типографика будет плотнее.

## 7. Что проверено и как

- `pnpm check` (tsc, включая `api/`) — без ошибок.
- `pnpm build` — проходит, sitemap и robots попадают в `dist/public`.
- Скриншоты и замеры на 1440 / 1280 / 390 для `/`, `/cases`, `/cases/k1-beton`:
  горизонтального скролла нет ни на одном.
- Форма: ошибки валидации показываются у поля, введённые данные **не
  сбрасываются**; UTM (`utm_source/medium/campaign`) и referer доезжают до
  payload; success-state называет канал и срок ответа; при недоступном
  endpoint появляется письмо с уже подставленным текстом.
- Аналитика: на живой странице зафиксированы `hero_cases_click`,
  `service_open`, `contact_start` и `scroll_depth` по всем секциям
  (`problems, services, cases, process, team, faq, contact` + 25/50/75/100%).
- Клавиатурный фокус: лаймовое кольцо `2px` на интерактивных элементах.

### Исправленные по ходу дефекты

| Дефект | Как проявлялся | Исправление |
|---|---|---|
| Карточки команды обрезались на мобильном | `min-width: auto` у grid-элементов раздвигал колонки за пределы контейнера, лишнее срезал `overflow: clip` | `min-width: 0` для элементов сеток |
| Пункты меню не помещались на мобильном | 7 пунктов с длинными подписями при `font-size: 11vw` | своя типографическая шкала для меню на ≤620px |
| `scroll_depth` не срабатывал для высоких секций | `threshold: 0.35` недостижим для секции выше ~2.8 экранов | `threshold: 0` + центрирующий `rootMargin` |
| «Все кейсы» наезжало на название клиента | `.text-link` — инлайновый элемент | блочная раскладка заголовка кейса |
| Подзаголовок финального CTA терялся | правило `.contact-title > p` перебивало по специфичности | повышена специфичность правила |
| 4-я обложка кейса не имела своего цвета | не было правила `.case-peach` | добавлено |
| Меню не закрывалось по Esc | подпись «press esc to close» была, обработчика не было | добавлен обработчик + `inert` для скрытого оверлея |
| Каждая загрузка страницы делала битый запрос | тег Umami с неподставленными `%VITE_…%` | тег подключается из кода только при заданных env |

## 8. Критерии приёмки (§12 ТЗ)

| # | Критерий | Статус |
|---|---|---|
| 1 | За 5 секунд понятно, что DMO делает B2B-системы | ✅ H1 + подзаголовок + микролейбл `B2B WEB SYSTEMS / AUTOMATION / E-COMMERCE` |
| 2 | ≥3 реальных кейса с задачей, решением и доказательством | ✅ 4 кейса, у каждого поле `proof` |
| 3 | В услугах — продуктовые категории | ✅ сайты/каталоги, кабинеты, CRM, интеграции |
| 4 | Процесс описывает работу с бизнес-процессом | ✅ «Разбираем процесс» → «Интегрируем и развиваем», у каждого шага «Ваш вклад» |
| 5 | Роль Арсения обновлена | ✅ `FOUNDER / SALES / CLIENT ACQUISITION` |
| 6 | FAQ закрывает интеграции, ТЗ, сроки, поддержку, NDA | ✅ 6 вопросов |
| 7 | Все CTA ведут в единый сценарий | ✅ все «Разобрать задачу» → `#contact` |
| 8 | Форма отправляется, сохраняет ввод, фиксирует источник | ⚠️ логика проверена целиком; фактическая доставка включится после §4.4 |
| 9 | На мобильном нет наложений и обрезанных карточек | ✅ проверено замерами на 390px |
| 10 | Нет неподтверждённых цифр, scarcity и NDA-деталей | ⚠️ вымышленные метрики удалены, NDA отмечен явно; «Доступные слоты: 02» ждёт ответа по §4.1 |

## 10. Пост-релизный круг (M1–M5)

Пять задач выполнены параллельно, каждая в отдельном git-worktree.

| Что | Результат |
|---|---|
| Сборка | `index.html` 368 963 → 4 800 Б; JS 394 648 → **295 434 Б** (gzip 117,9 → 92,3 кБ) |
| Чистка | CSS 131 776 → **44 614 Б** (−66%); `node_modules` 464 → 167 МБ; −63 файла, −52 зависимости |
| Пререндер | статический HTML с верными мета-тегами для `/cases` и каждого кейса |
| Тесты | 91 тест: инварианты кейсов, атрибуция, серверная валидация заявки |
| Шрифты и иконки | загрузка перенесена в `<head>`, появились favicon, apple-touch-icon, manifest |
| Доступность | сплошная иерархия заголовков, объявление ошибок формы, фокус при смене маршрута |
| Контраст | нарушений AA: было 26/11/7 по трём страницам, стало 6/0/0 |

Найдено и исправлено попутно:
- `buildMailtoFallback` вырезал пустую строку-разделитель тем же фильтром,
  что и пропущенное поле «Сайт» — в письме её никогда не было;
- фокус на невалидное поле формы переводился до коммита React, поэтому
  скринридер ошибку не произносил;
- `SiteShell` размонтируется на каждой странице, из-за чего флаг «первый
  рендер» не работал и после перехода фокус оставался на `body`;
- `<meta name="viewport">` содержал `maximum-scale=1`, запрещавший
  масштабирование на мобильных (WCAG 1.4.4).

## 9. Следующие шаги

1. Ответить по §4 (слоты, роли, фото, канал заявок, аналитика, домен).
2. Положить фотографии в `client/public/team/`.
3. Задать пару Telegram-переменных в Vercel (§4.0) и проверить сквозной
   путь заявки на проде.
5. Собрать карточки дополнительных кейсов по шаблону из 11 полей —
   и снять с них `status: "draft"`.
6. Опционально: скриншоты реальных систем как обложки кейсов вместо
   генеративной графики.
